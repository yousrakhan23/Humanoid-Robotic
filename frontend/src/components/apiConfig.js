// API Configuration
const API_CONFIG = {
  // This will be updated during deployment with the actual backend URL
  // For separate deployments, we'll use relative paths since the proxy is now in place
  BACKEND_URL: ''
};

export default API_CONFIG;